-- colors/one-dark-islands.lua
-- Entry point invoked by `:colorscheme one-dark-islands`.
-- Loads the theme using whatever config was set via setup() (or defaults).

require("one-dark-islands").load()
