# ~/.config/tmux/tmux.conf
# Managed via GNU Stow → ~/personal/dotfiles/tmux/.config/tmux/tmux.conf
#
# Plugin manager : TPM  (bootstrap → see bottom of file)
# Navigator      : christoomey/vim-tmux-navigator  (also install nvim side)
# Status bar     : catppuccin/tmux v2  (mocha palette)
#
# Module toggle pattern
#   status-right is a plain string — add/remove #{E:@catppuccin_status_<module>}
#   tokens to enable/disable each widget. No function calls, no conditionals needed.

# ─── Terminal & colour ────────────────────────────────────────────────────────
# WezTerm can advertise itself as either "xterm-256color" (its default) or
# "wezterm" (set `term = "wezterm"` in your .wezterm.lua to unlock undercurl,
# hyperlinks, and other extended capabilities).
# Both patterns are covered below — whichever $TERM WezTerm presents, tmux
# will pass true-colour and the extended feature set through to nvim.
set -g  default-terminal   "tmux-256color"
set -as terminal-features  ",xterm-256color:RGB:hyperlinks:usstyle"
set -as terminal-features  ",wezterm:RGB:hyperlinks:usstyle"
set -as terminal-overrides ",xterm-256color:Tc"
set -as terminal-overrides ",wezterm:Tc"

# ─── Core behaviour ───────────────────────────────────────────────────────────
set -g  escape-time        0       # no Esc delay — critical for nvim
set -g  focus-events       on      # pass FocusIn/Out to editors (gitsigns, etc.)
set -g  mouse              on
set -g  history-limit      50000
set -g  base-index         1       # windows start at 1
setw -g pane-base-index    1
set -g  renumber-windows   on      # collapse gaps after window close
set -g  set-titles         on
set -g  set-titles-string  "#S · #W"

# ─── Status bar position ──────────────────────────────────────────────────────
set -g status-position top
set -g status-interval 5           # balance refresh vs shell cost (cpu / net-speed / battery)

# ─── Splits & windows (keep cwd) ─────────────────────────────────────────────
unbind '"'
unbind %
bind \\ split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window      -c "#{pane_current_path}"

# ─── Pane resize (repeatable) ────────────────────────────────────────────────
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# ─── Copy mode (vi keys) ──────────────────────────────────────────────────────
setw -g mode-keys vi
bind Enter copy-mode
bind -T copy-mode-vi v      send -X begin-selection
bind -T copy-mode-vi y      send -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode-vi Escape send -X cancel

# ─── Misc bindings ────────────────────────────────────────────────────────────
bind r source-file "$TMUX_CONF" \; display "Config reloaded ✓"
bind x kill-pane                   # skip the confirmation prompt
bind q display-panes               # briefly show pane numbers

# ─── Plugins ──────────────────────────────────────────────────────────────────
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'   # sane defaults (overrideable below)

# Seamless pane ↔ nvim window navigation
# Requires nvim plugin: christoomey/vim-tmux-navigator
# See nvim spec: custom/plugins/vim-tmux-navigator.lua
set -g @plugin 'christoomey/vim-tmux-navigator'

# Status bar theme
set -g @plugin 'catppuccin/tmux#v2.1.3'

# Data sources used by catppuccin modules
set -g @plugin 'tmux-plugins/tmux-cpu'          # cpu, memory, cpu_temp modules
set -g @plugin 'tmux-plugins/tmux-battery'       # battery module
set -g @plugin 'tmux-plugins/tmux-net-speed'     # net_speed module

# ─── catppuccin/tmux config ───────────────────────────────────────────────────
set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_status_background "none"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_status_left_separator ""
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_middle_separator ""

# Window status style: rounded | basic | slanted | custom
set -g @catppuccin_window_status_style "basic"
set -g @catppuccin_window_default_text  " #W"
set -g @catppuccin_window_current_text  " #W"
set -g @catppuccin_window_current_fill  "number"

# ─── Status left ──────────────────────────────────────────────────────────────
# Shows: session name
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-left-length 60

# ─── Status right — MODULE TOGGLES ────────────────────────────────────────────
# Comment out any #{E:@catppuccin_status_*} token to disable that module.
# Order here = order on screen (left → right).
#
#   cpu / battery  catppuccin built-ins (tmux/status/*.conf) + tmux-cpu / tmux-battery
#   memory         custom (status_pills/memory.conf)
#   network        custom (status_pills/network.conf)
#
set -g status-right "\
#{E:@catppuccin_status_cpu}\
#{E:@catppuccin_status_memory}\
#{E:@catppuccin_status_network}\
#{E:@catppuccin_status_battery}"
set -g status-right-length 250

# ─── Status bar background ────────────────────────────────────────────────────
# "default" tells tmux not to paint any background — the terminal's own
# background colour shows through instead.
# Must come AFTER the @catppuccin_* options so it overrides the theme's surface.
# To restore catppuccin's solid background, comment all three lines out.
# set -g status-style                "bg=default"
# set -g window-status-style         "bg=default"
# set -g window-status-current-style "bg=default"

# Built-in cpu/battery: catppuccin/tmux status/*.conf (after TPM; see end).
# Custom memory/network: status_pills/*.conf

# tmux-net-speed formatting
set -g @net_speed_interfaces  "auto"
set -g @download_speed_format "%8s"
set -g @upload_speed_format   "%8s"

# ─── Pane borders ─────────────────────────────────────────────────────────────
set -g pane-border-style          "fg=#{@thm_surface0}"
set -g pane-active-border-style   "fg=#{@thm_lavender}"

# ─── TPM bootstrap ────────────────────────────────────────────────────────────
# If ~/.tmux/plugins/tpm does not exist, clone it and immediately install every
# @plugin listed in this file — no manual step required on a fresh machine.
# On all subsequent starts TPM is already present and this if-branch is skipped.
if "test ! -d ~/.tmux/plugins/tpm" \
  "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && \
        ~/.tmux/plugins/tpm/bin/install_plugins'"

# Auto-install any *newly added* @plugin lines whenever a session is created.
# Zero cost on normal launches (install_plugins is a no-op when nothing is missing).
set-hook -g session-created "run-shell '~/.tmux/plugins/tpm/bin/install_plugins'"

# tmux-battery replaces #{battery_*} inside listed options (not only status-right).
# Catppuccin keeps those tokens inside @catppuccin_status_battery — register it here.
set -g @plugin_interpolated_options "status-right status-left @catppuccin_status_battery @catppuccin_battery_icon @catppuccin_battery_text"

# Load TPM — almost last: status modules must run after the theme loads.
# Manual shortcuts (still available):
#   prefix + I       install missing plugins
#   prefix + U       upgrade all plugins
#   prefix + Alt-u   remove plugins no longer in the list
run '~/.tmux/plugins/tpm/tpm'

# Catppuccin stock modules (upstream: catppuccin/tmux/status/*.conf).
# Override built-in literals so tmux-cpu/tmux-battery interpolation can execute.
# (Upstream uses #{l:...} wrappers, which keep values as text in this setup.)
set -g @catppuccin_status_cpu_text_fg "#{cpu_fg_color}"
set -g @catppuccin_status_cpu_text_bg "#{cpu_bg_color}"
set -g @catppuccin_cpu_text " #{cpu_percentage}"
set -g @catppuccin_battery_icon "#{battery_icon} "
set -g @catppuccin_battery_text " #{battery_percentage}"
source-file -F "#{TMUX_PLUGIN_MANAGER_PATH}tmux/status/cpu.conf"
source-file -F "#{TMUX_PLUGIN_MANAGER_PATH}tmux/status/battery.conf"
# Custom pills (memory, network); %hidden MODULE_NAME matches catppuccin pattern.
source-file -F "#{d:current_file}/status_pills/memory.conf"
source-file -F "#{d:current_file}/status_pills/network.conf"

# Built-in cpu/battery modules embed #{cpu_*}/#{battery_*}; plugins normally only
# rewrite status-right/left. Re-run interpolation after module options exist.
run-shell "#{TMUX_PLUGIN_MANAGER_PATH}tmux-battery/battery.tmux"
run-shell 'bash "$HOME/.config/tmux/scripts/interpolate_catppuccin_cpu.sh"'

